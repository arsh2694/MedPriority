# =============================================================================
# MedPriority Backend — Python Dependencies
# =============================================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Settings management (loads .env into Python config)
pydantic-settings==2.2.1

# Database ORM and MySQL driver (Day 3)
sqlalchemy>=2.0.52
alembic>=1.13.1
pymysql==1.1.0
cryptography==42.0.5
bcrypt==4.0.1

# Authentication (Day 2 — Week 2)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Form data parsing (needed for some FastAPI endpoints)
python-multipart==0.0.9

# Rate limiting (Week 6)
slowapi==0.1.9

# Firebase Cloud Messaging (Week 6)
firebase-admin==6.5.0

# HTTP client for internal requests
httpx==0.27.0

# Background task scheduler — session expiry (Week 3)
apscheduler==3.10.4
